#include <iostream>
using namespace std;

int main() {
	double seconds, minutes, hours;
	cin >> seconds;
	hours = seconds / 3600;
	minutes = seconds / 60;
	cout << hours << ":" << minutes << ":" << seconds;
	return 0;
}
